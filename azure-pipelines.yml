pr:
  branches:
    include:
      - main

pool:
  name: Azure Pipelines
  vmImage: ubuntu-latest

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'clean test'
    jdkVersionOption: '11'
